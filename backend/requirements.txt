# ── Janus backend requirements — HF Spaces compatible ────────────────────────
# Using >= constraints (not pinned) so Docker resolves fresh compatible set

# Web framework
fastapi>=0.115.0
uvicorn[standard]>=0.30.0
pydantic>=2.9.0
pydantic-settings>=2.5.0
python-multipart>=0.0.9

# HTTP client
httpx>=0.27.0

# AI / LLM
langchain>=0.3.0
langchain-core>=0.3.0
langchain-community>=0.3.0
langgraph>=0.2.0

# LLM providers (optional — degrade gracefully if not installed)
google-generativeai>=0.8.0
openai>=1.0.0

# Market data
yfinance>=0.2.0

# HF Spaces persistence
huggingface_hub>=0.26.0

# Research / search
tavily-python>=0.5.0
playwright>=1.52.0
markdownify>=0.13.1

# Utilities
python-dotenv>=1.0.0
aiofiles>=24.0.0
tenacity>=8.0.0

# Data processing
numpy>=1.24.0
